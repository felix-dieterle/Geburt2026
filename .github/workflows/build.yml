name: Build and Release APK

on:
  push:
    branches:
      - main

permissions:
  contents: write

jobs:
  build:
    name: Build Release APK
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Grant execute permission for gradlew
        run: chmod +x gradlew

      - name: Build Release APK
        run: ./gradlew assembleRelease

      - name: Get build date and short SHA
        id: meta
        run: |
          echo "date=$(date +'%Y-%m-%d_%H-%M')" >> $GITHUB_OUTPUT
          echo "sha=$(git rev-parse --short HEAD)" >> $GITHUB_OUTPUT

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: "build-${{ steps.meta.outputs.date }}-${{ steps.meta.outputs.sha }}"
          name: "Geburt2026 App – ${{ steps.meta.outputs.date }}"
          body: |
            ## 🍼 Geburt2026 – Geburtsbegleitung App

            **Automatisch gebaut am:** ${{ steps.meta.outputs.date }}
            **Commit:** ${{ steps.meta.outputs.sha }}

            ### Installation
            1. APK-Datei herunterladen
            2. Auf Android: Einstellungen → Unbekannte Apps → Installieren erlauben
            3. APK öffnen und installieren

            ### Features
            - ⏱️ Blasensprung-Timer (seit 22.02.2026, 06:15 Uhr)
            - 🏥 Hep-B Hinweise für die Geburtsklinik
            - 👨‍👩‍👧‍👦 Kinder & Transport-Koordination
            - ✅ Checkliste für die nächsten 4 Tage
            - 📞 Notfallkontakte
          files: app/build/outputs/apk/release/app-release.apk
          draft: false
          prerelease: false
